--- a/arch/arm64/boot/dts/mediatek/mt6363.dtsi
+++ b/arch/arm64/boot/dts/mediatek/mt6363.dtsi
@@ -9,6 +9,22 @@
 	interrupt-controller;
 	#interrupt-cells = <2>;
 
+	regulators {
+		compatible = "mediatek,mt6363-regulator";
+
+		mt6363_vcn13: vcn13 {
+			regulator-name = "mt6363-vcn13";
+			regulator-min-microvolt = <900000>;
+			regulator-max-microvolt = <1810000>;
+		};
+
+		mt6363_vrfio18: vrfio18 {
+			regulator-name = "mt6363-vrfio18";
+			regulator-min-microvolt = <600000>;
+			regulator-max-microvolt = <2100000>;
+		};
+	};
+
 	mt6363_keys: keys {
 		compatible = "mediatek,mt6363-keys";
 
--- a/drivers/mfd/mt6363-spmi.c
+++ b/drivers/mfd/mt6363-spmi.c
@@ -50,6 +50,10 @@
 		.resources = mt6363_key_resources,
 		.num_resources = ARRAY_SIZE(mt6363_key_resources),
 	},
+	{
+		.name = "mt6363-regulator",
+		.of_compatible = "mediatek,mt6363-regulator",
+	},
 };
 
 static void mt6363_irq_enable(struct irq_data *data)
--- a/drivers/regulator/Kconfig
+++ b/drivers/regulator/Kconfig
@@ -944,6 +944,14 @@
 	  2-channel buck with Thermal Shutdown and Overload Protection
 	  6-channel High PSRR and Low Dropout LDO.
 
+config REGULATOR_MT6363
+	tristate "MediaTek MT6363 connectivity regulators"
+	depends on MFD_MT6363
+	help
+	  Enable the VCN13 and VRFIO18 LDOs used by the MT6878
+	  connectivity subsystem. Consumers remain disabled until their
+	  device-tree nodes explicitly reference these supplies.
+
 config REGULATOR_MT6369
 	tristate "MediaTek MT6369 PMIC regulators"
 	depends on MFD_MT6369
--- a/drivers/regulator/Makefile
+++ b/drivers/regulator/Makefile
@@ -112,6 +112,7 @@
 obj-$(CONFIG_REGULATOR_MT6358)	+= mt6358-regulator.o
 obj-$(CONFIG_REGULATOR_MT6359)	+= mt6359-regulator.o
 obj-$(CONFIG_REGULATOR_MT6360) += mt6360-regulator.o
+obj-$(CONFIG_REGULATOR_MT6363) += mt6363-regulator.o
 obj-$(CONFIG_REGULATOR_MT6369) += mt6369-regulator.o
 obj-$(CONFIG_REGULATOR_MT6370) += mt6370-regulator.o
 obj-$(CONFIG_REGULATOR_MT6380)	+= mt6380-regulator.o
--- /dev/null
+++ b/drivers/regulator/mt6363-regulator.c
@@ -0,0 +1,107 @@
+// SPDX-License-Identifier: GPL-2.0
+/*
+ * Minimal regulator support for MediaTek MT6363 connectivity rails.
+ */
+
+#include <linux/mfd/mt6363/registers.h>
+#include <linux/module.h>
+#include <linux/of.h>
+#include <linux/platform_device.h>
+#include <linux/regmap.h>
+#include <linux/regulator/driver.h>
+
+static const unsigned int mt6363_vcn13_voltages[] = {
+	900000, 1000000, 1100000, 1200000,
+	1300000, 1700000, 1800000, 1810000,
+};
+
+static const unsigned int mt6363_vrfio18_voltages[] = {
+	600000, 700000, 800000, 900000,
+	1000000, 1100000, 1200000, 1300000,
+	1400000, 1500000, 1600000, 1700000,
+	1800000, 1900000, 2000000, 2100000,
+};
+
+static const struct regulator_ops mt6363_ldo_ops = {
+	.enable = regulator_enable_regmap,
+	.disable = regulator_disable_regmap,
+	.is_enabled = regulator_is_enabled_regmap,
+	.list_voltage = regulator_list_voltage_table,
+	.get_voltage_sel = regulator_get_voltage_sel_regmap,
+	.set_voltage_sel = regulator_set_voltage_sel_regmap,
+};
+
+static const struct regulator_desc mt6363_regulators[] = {
+	{
+		.name = "vcn13",
+		.of_match = "vcn13",
+		.ops = &mt6363_ldo_ops,
+		.type = REGULATOR_VOLTAGE,
+		.owner = THIS_MODULE,
+		.volt_table = mt6363_vcn13_voltages,
+		.n_voltages = ARRAY_SIZE(mt6363_vcn13_voltages),
+		.enable_reg = MT6363_VCN13_ENABLE,
+		.enable_mask = BIT(0),
+		.vsel_reg = MT6363_VCN13_VSEL,
+		.vsel_mask = GENMASK(3, 0),
+	},
+	{
+		.name = "vrfio18",
+		.of_match = "vrfio18",
+		.ops = &mt6363_ldo_ops,
+		.type = REGULATOR_VOLTAGE,
+		.owner = THIS_MODULE,
+		.volt_table = mt6363_vrfio18_voltages,
+		.n_voltages = ARRAY_SIZE(mt6363_vrfio18_voltages),
+		.enable_reg = MT6363_VRFIO18_ENABLE,
+		.enable_mask = BIT(0),
+		.vsel_reg = MT6363_VRFIO18_VSEL,
+		.vsel_mask = GENMASK(3, 0),
+	},
+};
+
+static int mt6363_regulator_probe(struct platform_device *pdev)
+{
+	struct regulator_config config = {};
+	struct regmap *regmap;
+	unsigned int i;
+
+	regmap = dev_get_regmap(pdev->dev.parent, NULL);
+	if (!regmap)
+		return dev_err_probe(&pdev->dev, -ENODEV,
+				     "failed to get parent regmap\n");
+
+	config.dev = &pdev->dev;
+	config.regmap = regmap;
+
+	for (i = 0; i < ARRAY_SIZE(mt6363_regulators); i++) {
+		struct regulator_dev *rdev;
+
+		rdev = devm_regulator_register(&pdev->dev,
+					      &mt6363_regulators[i], &config);
+		if (IS_ERR(rdev))
+			return dev_err_probe(&pdev->dev, PTR_ERR(rdev),
+					     "failed to register %s\n",
+					     mt6363_regulators[i].name);
+	}
+
+	return 0;
+}
+
+static const struct of_device_id mt6363_regulator_of_match[] = {
+	{ .compatible = "mediatek,mt6363-regulator" },
+	{ }
+};
+MODULE_DEVICE_TABLE(of, mt6363_regulator_of_match);
+
+static struct platform_driver mt6363_regulator_driver = {
+	.probe = mt6363_regulator_probe,
+	.driver = {
+		.name = "mt6363-regulator",
+		.of_match_table = mt6363_regulator_of_match,
+	},
+};
+module_platform_driver(mt6363_regulator_driver);
+
+MODULE_DESCRIPTION("MediaTek MT6363 connectivity regulator driver");
+MODULE_LICENSE("GPL");
--- a/include/linux/mfd/mt6363/registers.h
+++ b/include/linux/mfd/mt6363/registers.h
@@ -8,4 +8,9 @@
 #define MT6363_PSC_TOP_INT_CON0			0x090f
 #define MT6363_PSC_TOP_INT_STATUS0		0x091b
 
+#define MT6363_VCN13_VSEL			0x1b3f
+#define MT6363_VRFIO18_ENABLE			0x1bcd
+#define MT6363_VCN13_ENABLE			0x1d07
+#define MT6363_VRFIO18_VSEL			0x1f2c
+
 #endif

--- a/drivers/soc/mediatek/Kconfig
+++ b/drivers/soc/mediatek/Kconfig
@@ -83,6 +83,17 @@ config MTK_SVS
 	  which has several controllers(banks) for calculating suitable
 	  voltage to different power domains(CPU/GPU/CCI) according to
 	  chip process corner, temperatures and other factors. Then DVFS
 	  driver could apply SVS bank voltage to PMIC/Buck.
+
+config MTK_MT6878_CONSYS
+	tristate "MediaTek MT6878 connectivity bring-up driver"
+	depends on ARCH_MEDIATEK || COMPILE_TEST
+	depends on PM
+	help
+	  Minimal MT6878/MT6631 connectivity subsystem bring-up driver.
+	  This only controls the CONN power domain and connectivity rails
+	  and exposes a manual sysfs power switch for hardware validation.
+	  Wi-Fi, Bluetooth and GNSS client drivers still need separate WMT
+	  and protocol driver support.
 
 config MTK_SOCINFO
 	tristate "MediaTek SoC Information"
--- a/drivers/soc/mediatek/Makefile
+++ b/drivers/soc/mediatek/Makefile
@@ -9,3 +9,4 @@ obj-$(CONFIG_MTK_MMSYS) += mtk-mmsys.o
 obj-$(CONFIG_MTK_MMSYS) += mtk-mutex.o
 obj-$(CONFIG_MTK_SVS) += mtk-svs.o
 obj-$(CONFIG_MTK_SOCINFO) += mtk-socinfo.o
+obj-$(CONFIG_MTK_MT6878_CONSYS) += mtk-mt6878-consys.o
--- /dev/null
+++ b/drivers/soc/mediatek/mtk-mt6878-consys.c
@@ -0,0 +1,469 @@
+// SPDX-License-Identifier: GPL-2.0-only
+/*
+ * Minimal MediaTek MT6878 / MT6631 connectivity subsystem bring-up.
+ */
+
+#include <linux/bits.h>
+#include <linux/delay.h>
+#include <linux/io.h>
+#include <linux/kernel.h>
+#include <linux/kstrtox.h>
+#include <linux/module.h>
+#include <linux/mutex.h>
+#include <linux/of.h>
+#include <linux/platform_device.h>
+#include <linux/pm_runtime.h>
+#include <linux/regulator/consumer.h>
+#include <linux/slab.h>
+#include <linux/string.h>
+#include <linux/sysfs.h>
+
+enum mt6878_consys_reg {
+	CONSYS_RGU_ON,
+	CONSYS_INFRACFG_AO,
+	CONSYS_CFG_ON,
+	CONSYS_WT_SLP,
+	CONSYS_BUS_CR_ON,
+	CONSYS_CFG,
+	CONSYS_HOST_CSR,
+	CONSYS_SPM,
+	CONSYS_REG_MAX,
+};
+
+struct mt6878_consys_regmap {
+	unsigned int dt_index;
+	const char *name;
+};
+
+static const struct mt6878_consys_regmap mt6878_consys_regs[] = {
+	[CONSYS_RGU_ON] = { 0, "rgu_on" },
+	[CONSYS_INFRACFG_AO] = { 1, "infracfg_ao" },
+	[CONSYS_CFG_ON] = { 4, "cfg_on" },
+	[CONSYS_WT_SLP] = { 5, "wt_slp" },
+	[CONSYS_BUS_CR_ON] = { 6, "bus_cr_on" },
+	[CONSYS_CFG] = { 7, "cfg" },
+	[CONSYS_HOST_CSR] = { 20, "host_csr" },
+	[CONSYS_SPM] = { 22, "spm" },
+};
+
+#define MT6878_CONSYS_POWERON_CONFIG_EN		0x000
+#define MT6878_CONSYS_CONN_PWR_CON		0xe04
+#define MT6878_CONSYS_INFRA_PROT_EN_CLR_0	0xc48
+#define MT6878_CONSYS_INFRA_PROT_RDY_STA_0	0xc4c
+#define MT6878_CONSYS_INFRA_PROT_EN_CLR_1	0xc58
+#define MT6878_CONSYS_INFRA_PROT_RDY_STA_1	0xc5c
+#define MT6878_CONSYS_MCU_PROT_EN_CLR_0		0xc98
+#define MT6878_CONSYS_MCU_PROT_RDY_STA_0		0xc9c
+#define MT6878_CONSYS_CONN_HW_VER		0x02050401
+
+struct mt6878_consys_supply {
+	const char *name;
+	int min_uv;
+	int max_uv;
+	struct regulator *regulator;
+};
+
+struct mt6878_consys {
+	struct device *dev;
+	void __iomem *regs[CONSYS_REG_MAX];
+	struct mt6878_consys_supply supplies[4];
+	struct mutex lock;
+	bool powered;
+};
+
+static int mt6878_consys_get_supplies(struct mt6878_consys *cs)
+{
+	static const struct mt6878_consys_supply init[] = {
+		{ "vcn13", 1300000, 1700000 },
+		{ "vrfio18", 1700000, 1700000 },
+		{ "vcn33-1", 3300000, 3300000 },
+		{ "vcn33-2", 3300000, 3300000 },
+	};
+	unsigned int i;
+	int ret;
+
+	memcpy(cs->supplies, init, sizeof(init));
+
+	for (i = 0; i < ARRAY_SIZE(cs->supplies); i++) {
+		cs->supplies[i].regulator =
+			devm_regulator_get_optional(cs->dev, cs->supplies[i].name);
+		if (IS_ERR(cs->supplies[i].regulator)) {
+			ret = PTR_ERR(cs->supplies[i].regulator);
+			if (ret == -ENODEV) {
+				dev_dbg(cs->dev, "optional supply %s missing\n",
+					cs->supplies[i].name);
+				cs->supplies[i].regulator = NULL;
+				continue;
+			}
+
+			return dev_err_probe(cs->dev, ret,
+					     "failed to get %s supply\n",
+					     cs->supplies[i].name);
+		}
+	}
+
+	return 0;
+}
+
+static int mt6878_consys_enable_supplies(struct mt6878_consys *cs)
+{
+	unsigned int i;
+	int ret;
+
+	for (i = 0; i < ARRAY_SIZE(cs->supplies); i++) {
+		struct mt6878_consys_supply *supply = &cs->supplies[i];
+
+		if (!supply->regulator)
+			continue;
+
+		ret = regulator_set_voltage(supply->regulator, supply->min_uv,
+					    supply->max_uv);
+		if (ret)
+			goto err_disable;
+
+		ret = regulator_enable(supply->regulator);
+		if (ret)
+			goto err_disable;
+	}
+
+	return 0;
+
+err_disable:
+	while (i--) {
+		if (cs->supplies[i].regulator)
+			regulator_disable(cs->supplies[i].regulator);
+	}
+
+	return ret;
+}
+
+static void mt6878_consys_disable_supplies(struct mt6878_consys *cs)
+{
+	int i;
+
+	for (i = ARRAY_SIZE(cs->supplies) - 1; i >= 0; i--) {
+		if (cs->supplies[i].regulator)
+			regulator_disable(cs->supplies[i].regulator);
+	}
+}
+
+static u32 mt6878_consys_read(struct mt6878_consys *cs,
+			      enum mt6878_consys_reg reg, u32 offset)
+{
+	return readl(cs->regs[reg] + offset);
+}
+
+static void mt6878_consys_write(struct mt6878_consys *cs,
+				enum mt6878_consys_reg reg, u32 offset, u32 val)
+{
+	writel(val, cs->regs[reg] + offset);
+}
+
+static void mt6878_consys_set_bits(struct mt6878_consys *cs,
+				   enum mt6878_consys_reg reg,
+				   u32 offset, u32 mask)
+{
+	mt6878_consys_write(cs, reg, offset,
+			    mt6878_consys_read(cs, reg, offset) | mask);
+}
+
+static void mt6878_consys_clear_bits(struct mt6878_consys *cs,
+				     enum mt6878_consys_reg reg,
+				     u32 offset, u32 mask)
+{
+	mt6878_consys_write(cs, reg, offset,
+			    mt6878_consys_read(cs, reg, offset) & ~mask);
+}
+
+static void mt6878_consys_update_bits(struct mt6878_consys *cs,
+				      enum mt6878_consys_reg reg,
+				      u32 offset, u32 mask, u32 val)
+{
+	u32 tmp = mt6878_consys_read(cs, reg, offset);
+
+	tmp &= ~mask;
+	tmp |= val & mask;
+	mt6878_consys_write(cs, reg, offset, tmp);
+}
+
+static int mt6878_consys_poll_bit(struct mt6878_consys *cs,
+				  enum mt6878_consys_reg reg,
+				  u32 offset, u32 bit, bool set,
+				  unsigned int tries, unsigned int delay_us)
+{
+	u32 val;
+
+	while (tries--) {
+		val = mt6878_consys_read(cs, reg, offset);
+		if (!!(val & BIT(bit)) == set)
+			return 0;
+
+		usleep_range(delay_us, delay_us + 100);
+	}
+
+	val = mt6878_consys_read(cs, reg, offset);
+	dev_err(cs->dev, "%s[0x%03x] bit %u did not become %u, val=0x%08x\n",
+		mt6878_consys_regs[reg].name, offset, bit, set, val);
+	return -ETIMEDOUT;
+}
+
+static int mt6878_consys_hw_power_on(struct mt6878_consys *cs)
+{
+	u32 chip_id;
+	int ret;
+
+	mt6878_consys_update_bits(cs, CONSYS_SPM,
+				  MT6878_CONSYS_POWERON_CONFIG_EN,
+				  0xffff0001, 0x0b160001);
+
+	mt6878_consys_set_bits(cs, CONSYS_SPM,
+			       MT6878_CONSYS_CONN_PWR_CON, BIT(2));
+	ret = mt6878_consys_poll_bit(cs, CONSYS_SPM,
+				     MT6878_CONSYS_CONN_PWR_CON,
+				     30, true, 10, 500);
+	if (ret)
+		return ret;
+
+	mt6878_consys_set_bits(cs, CONSYS_SPM,
+			       MT6878_CONSYS_CONN_PWR_CON, BIT(3));
+	ret = mt6878_consys_poll_bit(cs, CONSYS_SPM,
+				     MT6878_CONSYS_CONN_PWR_CON,
+				     31, true, 10, 500);
+	if (ret)
+		return ret;
+
+	mt6878_consys_clear_bits(cs, CONSYS_SPM,
+				 MT6878_CONSYS_CONN_PWR_CON, BIT(4));
+	udelay(1);
+	mt6878_consys_clear_bits(cs, CONSYS_SPM,
+				 MT6878_CONSYS_CONN_PWR_CON, BIT(1));
+	mt6878_consys_set_bits(cs, CONSYS_SPM,
+			       MT6878_CONSYS_CONN_PWR_CON, BIT(0));
+	usleep_range(500, 1000);
+
+	mt6878_consys_set_bits(cs, CONSYS_INFRACFG_AO,
+			       MT6878_CONSYS_MCU_PROT_EN_CLR_0, BIT(0));
+	ret = mt6878_consys_poll_bit(cs, CONSYS_INFRACFG_AO,
+				     MT6878_CONSYS_MCU_PROT_RDY_STA_0,
+				     0, false, 100, 500);
+	if (ret)
+		return ret;
+
+	mt6878_consys_set_bits(cs, CONSYS_INFRACFG_AO,
+			       MT6878_CONSYS_INFRA_PROT_EN_CLR_1, BIT(12));
+	ret = mt6878_consys_poll_bit(cs, CONSYS_INFRACFG_AO,
+				     MT6878_CONSYS_INFRA_PROT_RDY_STA_1,
+				     12, false, 100, 500);
+	if (ret)
+		return ret;
+
+	mt6878_consys_set_bits(cs, CONSYS_INFRACFG_AO,
+			       MT6878_CONSYS_INFRA_PROT_EN_CLR_0, BIT(8));
+	mt6878_consys_set_bits(cs, CONSYS_INFRACFG_AO,
+			       MT6878_CONSYS_MCU_PROT_EN_CLR_0, BIT(1));
+	msleep(5);
+
+	chip_id = mt6878_consys_read(cs, CONSYS_CFG, 0x0);
+	if (chip_id != MT6878_CONSYS_CONN_HW_VER)
+		return dev_err_probe(cs->dev, -ENODEV,
+				     "unexpected CONN HW version 0x%08x\n",
+				     chip_id);
+
+	return 0;
+}
+
+static int mt6878_consys_power_on(struct mt6878_consys *cs)
+{
+	int ret;
+
+	if (cs->powered)
+		return 0;
+
+	ret = mt6878_consys_enable_supplies(cs);
+	if (ret)
+		return ret;
+
+	ret = pm_runtime_resume_and_get(cs->dev);
+	if (ret)
+		goto err_supplies;
+
+	ret = mt6878_consys_hw_power_on(cs);
+	if (ret)
+		goto err_runtime;
+
+	usleep_range(2000, 3000);
+
+	dev_info(cs->dev,
+		 "powered on: pwr=0x%08x rgu=0x%08x cfg=0x%08x chip=0x%08x wt_slp=0x%08x host=0x%08x\n",
+		 mt6878_consys_read(cs, CONSYS_SPM,
+				    MT6878_CONSYS_CONN_PWR_CON),
+		 mt6878_consys_read(cs, CONSYS_RGU_ON, 0x0),
+		 mt6878_consys_read(cs, CONSYS_CFG_ON, 0x0),
+		 mt6878_consys_read(cs, CONSYS_CFG, 0x0),
+		 mt6878_consys_read(cs, CONSYS_WT_SLP, 0x0),
+		 mt6878_consys_read(cs, CONSYS_HOST_CSR, 0x0));
+
+	cs->powered = true;
+	return 0;
+
+err_runtime:
+	pm_runtime_put_sync(cs->dev);
+
+err_supplies:
+	mt6878_consys_disable_supplies(cs);
+	return ret;
+}
+
+static void mt6878_consys_power_off(struct mt6878_consys *cs)
+{
+	if (!cs->powered)
+		return;
+
+	pm_runtime_put_sync(cs->dev);
+	mt6878_consys_disable_supplies(cs);
+	cs->powered = false;
+	dev_info(cs->dev, "powered off\n");
+}
+
+static ssize_t power_on_show(struct device *dev,
+			     struct device_attribute *attr, char *buf)
+{
+	struct mt6878_consys *cs = dev_get_drvdata(dev);
+
+	return sysfs_emit(buf, "%u\n", cs->powered);
+}
+
+static ssize_t power_on_store(struct device *dev,
+			      struct device_attribute *attr,
+			      const char *buf, size_t count)
+{
+	struct mt6878_consys *cs = dev_get_drvdata(dev);
+	bool enable;
+	int ret;
+
+	ret = kstrtobool(buf, &enable);
+	if (ret)
+		return ret;
+
+	mutex_lock(&cs->lock);
+	if (enable)
+		ret = mt6878_consys_power_on(cs);
+	else
+		mt6878_consys_power_off(cs);
+	mutex_unlock(&cs->lock);
+
+	return ret ? ret : count;
+}
+static DEVICE_ATTR_RW(power_on);
+
+static ssize_t registers_show(struct device *dev,
+			      struct device_attribute *attr, char *buf)
+{
+	struct mt6878_consys *cs = dev_get_drvdata(dev);
+	ssize_t len = 0;
+
+	mutex_lock(&cs->lock);
+	len += sysfs_emit_at(buf, len, "powered: %u\n", cs->powered);
+	if (cs->powered) {
+		len += sysfs_emit_at(buf, len, "rgu_on[0x000]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_RGU_ON, 0x0));
+		len += sysfs_emit_at(buf, len, "rgu_on[0x010]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_RGU_ON, 0x10));
+		len += sysfs_emit_at(buf, len, "spm[0xe04]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_SPM,
+							MT6878_CONSYS_CONN_PWR_CON));
+		len += sysfs_emit_at(buf, len, "cfg[0x000]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_CFG, 0x0));
+		len += sysfs_emit_at(buf, len, "cfg_on[0x000]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_CFG_ON, 0x0));
+		len += sysfs_emit_at(buf, len, "cfg_on[0x018]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_CFG_ON, 0x18));
+		len += sysfs_emit_at(buf, len, "wt_slp[0x000]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_WT_SLP, 0x0));
+		len += sysfs_emit_at(buf, len, "bus_cr_on[0x000]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_BUS_CR_ON, 0x0));
+		len += sysfs_emit_at(buf, len, "host_csr[0x000]=0x%08x\n",
+				     mt6878_consys_read(cs, CONSYS_HOST_CSR, 0x0));
+	}
+	mutex_unlock(&cs->lock);
+
+	return len;
+}
+static DEVICE_ATTR_RO(registers);
+
+static struct attribute *mt6878_consys_attrs[] = {
+	&dev_attr_power_on.attr,
+	&dev_attr_registers.attr,
+	NULL,
+};
+ATTRIBUTE_GROUPS(mt6878_consys);
+
+static int mt6878_consys_probe(struct platform_device *pdev)
+{
+	struct mt6878_consys *cs;
+	unsigned int i;
+	int ret;
+
+	cs = devm_kzalloc(&pdev->dev, sizeof(*cs), GFP_KERNEL);
+	if (!cs)
+		return -ENOMEM;
+
+	cs->dev = &pdev->dev;
+	mutex_init(&cs->lock);
+	platform_set_drvdata(pdev, cs);
+
+	for (i = 0; i < ARRAY_SIZE(mt6878_consys_regs); i++) {
+		cs->regs[i] = devm_platform_ioremap_resource(pdev,
+					mt6878_consys_regs[i].dt_index);
+		if (IS_ERR(cs->regs[i]))
+			return dev_err_probe(&pdev->dev, PTR_ERR(cs->regs[i]),
+					     "failed to map %s\n",
+					     mt6878_consys_regs[i].name);
+	}
+
+	ret = mt6878_consys_get_supplies(cs);
+	if (ret)
+		return ret;
+
+	pm_runtime_enable(&pdev->dev);
+	ret = device_add_groups(&pdev->dev, mt6878_consys_groups);
+	if (ret)
+		goto err_pm;
+
+	dev_info(&pdev->dev, "registered MT6878/MT6631 connsys bring-up device\n");
+	return 0;
+
+err_pm:
+	pm_runtime_disable(&pdev->dev);
+	return ret;
+}
+
+static void mt6878_consys_remove(struct platform_device *pdev)
+{
+	struct mt6878_consys *cs = platform_get_drvdata(pdev);
+
+	device_remove_groups(&pdev->dev, mt6878_consys_groups);
+	mutex_lock(&cs->lock);
+	mt6878_consys_power_off(cs);
+	mutex_unlock(&cs->lock);
+	pm_runtime_disable(&pdev->dev);
+}
+
+static const struct of_device_id mt6878_consys_of_match[] = {
+	{ .compatible = "mediatek,mt6878-6631-consys" },
+	{ }
+};
+MODULE_DEVICE_TABLE(of, mt6878_consys_of_match);
+
+static struct platform_driver mt6878_consys_driver = {
+	.probe = mt6878_consys_probe,
+	.remove = mt6878_consys_remove,
+	.driver = {
+		.name = "mtk-mt6878-consys",
+		.of_match_table = mt6878_consys_of_match,
+	},
+};
+module_platform_driver(mt6878_consys_driver);
+
+MODULE_DESCRIPTION("MediaTek MT6878 connectivity subsystem bring-up driver");
+MODULE_LICENSE("GPL");
