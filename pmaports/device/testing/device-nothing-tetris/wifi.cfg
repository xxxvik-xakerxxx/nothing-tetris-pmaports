# Keep production logs to errors, warnings, state transitions and events.
# INFO/TRACE logging from the vendor WLAN driver can otherwise overwrite the
# early boot log within minutes while the interface is associated.
DbgLevel0 0xffffffff 0x0f
